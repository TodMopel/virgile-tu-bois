import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.coucourecords.virgiletubois",
  appName: "Sables d'O-Zone",
  webDir: "dist",
};

export default config;

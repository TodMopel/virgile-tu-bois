import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.coucourecords.virgiletubois",
  appName: "Virgile Tu Bois",
  webDir: "dist",
};

export default config;
